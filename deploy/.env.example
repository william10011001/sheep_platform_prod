# ===== Required =====
POSTGRES_PASSWORD=change_me_to_a_strong_password

# ===== Optional (bootstrap admin on first boot) =====
SHEEP_BOOTSTRAP_ADMIN_USER=sheep
SHEEP_BOOTSTRAP_ADMIN_PASS=@@Wm105020

# ===== Captcha (Streamlit slider captcha) =====
# SHEEP_CAPTCHA=1
# SHEEP_CAPTCHA_MIN_S=0.8

# ===== Notes =====
# 1) Edit deploy/nginx/conf.d/app.conf and replace YOUR_DOMAIN.
# 2) Edit deploy/nginx/html/index.html and replace YOUR_DOMAIN in og:url / og:image.
# 3) Issue cert with certbot (see README in this package).
